#!/bin/bash
NAME="$1"
shift
POP_UP="$1"
shift
TITLE="$1"
shift
BODY="$1"
shift
echo "$@" "$NAME":"$TITLE":"$BODY" | /usr/local/bin/memoplex -w
# todo: handle this again
#if [[ "$POP_UP" -eq 1 ]]
#then
#  echo >> ~/log/notifyPopup
#fi
