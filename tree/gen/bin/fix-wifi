#!/bin/sh
set -e
#sudo systemctl start wpa_supplicant.service
#nmcli dev wifi list
#nmcli --ask connection up #PUT-CONN-NAME-HERE#

sudo systemctl stop wpa_supplicant.service
sudo wpa_supplicant -i wlan0 -c /etc/wpa_supplicant.conf -B
sudo dhclient wlan0
