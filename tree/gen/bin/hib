#!/bin/sh
#sudo pm-hibernate
#sudo sh -c 'echo disk > /sys/power/state'
adjvol -m
#PM_DEBUG="true" PM_PROFILE="true" sudo pm-hibernate
sudo pm-hibernate
asdf
adjvol -u
sleep 3
think-touch off
#synctime > /dev/null 2>&1 &
