#!/bin/bash
# start an xt on the given screen window
n="$1"
t="$2"
shift 2
if [ "x$1" = "x--alternate" ]
then
    shift
    exec xt --alternate -T "$t" "$@" -e s "$n" &
else
    exec xt -T "$t" "$@" -e s "$n" &
fi
